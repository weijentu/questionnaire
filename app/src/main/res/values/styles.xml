<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Risk Evaluation start-->
    <style name="risk_theme_title">
        <item name="android:textSize">@dimen/text_size_18</item>
        <item name="android:textColor">@color/tc1</item>

        <item name="android:paddingRight">@dimen/XL</item>
        <item name="android:paddingTop">@dimen/XXXXXL</item>
        <item name="android:paddingBottom">@dimen/L</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/bc1</item>
    </style>


</resources>
